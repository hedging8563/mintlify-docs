---
title: "Lister les Lots"
openapi: "GET /v1/batches"
description: "Liste les travaux de lot pour l'organisation de la clé API actuelle."
---

## Aperçu

Cet endpoint fait partie de l'API Batch compatible avec OpenAI de AI Sonar. Les travaux de lot utilisent des fichiers JSONL téléchargés, s'exécutent de manière asynchrone et peuvent retourner des fichiers de sortie/d'erreur plus tard.

## Remarques

- Les fichiers d'entrée de lot doivent utiliser `purpose=batch`.
- `completion_window` est actuellement `24h`.
- L'ordre de sortie n'est pas garanti ; il faut toujours faire correspondre par `custom_id`.
- Le streaming n'est pas pris en charge à l'intérieur des éléments de lot.

## Exemple

<RequestExample>
```bash cURL
curl -X GET "https://api.aisonar.dev/v1/batches?limit=20" \
  -H "Authorization: Bearer sk-your-api-key"
```
</RequestExample>

## Demande / Réponse

Utilisez le panneau OpenAPI interactif ci-dessus pour le schéma exact.

## Conseils Opérationnels

- Utilisez `custom_id` pour la réconciliation idempotente en aval.
- Attendez-vous à ce que `output_file_id` et `error_file_id` ne soient disponibles qu'après que le travailleur finalise le lot.
- La tarification des lots peut différer de la tarification synchrone car les règles de remise `isBatchRequest=true` s'appliquent.

## Exemple de réponse

<ResponseExample>
```json 200 OK
{
  "object": "list",
  "data": [{ "id": "batch_abc123", "object": "batch", "status": "completed" }]
}
```
</ResponseExample>

## Champs importants

<ResponseField name="object" type="string">Type d’objet de la réponse.</ResponseField>
<ResponseField name="data" type="array">Liste ou payload retourné par cette réponse.</ResponseField>
<ResponseField name="status" type="string">État de cycle de vie actuel de cette ressource.</ResponseField>
