---
title: "Tác vụ bất đồng bộ và polling"
description: "Theo dõi workflow media, batch và task mà không phụ thuộc endpoint trạng thái riêng của provider."
---

## Tổng quan

Theo dõi workflow media, batch và task mà không phụ thuộc endpoint trạng thái riêng của provider.

Dùng API công khai và hợp đồng mô hình của AI Sonar; không phụ thuộc chi tiết thực thi của provider.

## Quy tắc chính

- Lưu `task_id`, `poll_url`, terminal status trước khi trả thành công cho người dùng.
- Dùng API công khai và hợp đồng mô hình của AI Sonar; không phụ thuộc chi tiết thực thi của provider.
- Làm retry có tính idempotent và tránh tạo hoặc tính phí trùng lặp.

## Checklist production

- Lưu `task_id`, `poll_url`, terminal status trước khi trả thành công cho người dùng.
- Dùng API công khai và hợp đồng mô hình của AI Sonar; không phụ thuộc chi tiết thực thi của provider.
- Làm retry có tính idempotent và tránh tạo hoặc tính phí trùng lặp.
- Log cần hữu ích cho support nhưng không lưu secret hoặc payload riêng tư.
- Xác minh hành vi ở staging trước khi đổi traffic production.

## Trang liên quan

- [Tác vụ bất đồng bộ và polling](/vi/guides/async-jobs-polling)
- [Quan sát và xử lý sự cố](/vi/guides/observability-troubleshooting)
- [Bảo mật và xử lý dữ liệu](/vi/guides/security-data-handling)
- [Models API](/vi/api-reference/models/list-models)
