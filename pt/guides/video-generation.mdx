---
title: "Geração de Vídeo"
description: "Gere vídeos com operações públicas explícitas, polling assíncrono e entradas de mídia específicas do modelo."
---

A geração de vídeo é assíncrona. `POST /v1/videos/generations` retorna uma identidade de tarefa pública e geralmente um `poll_url`; o vídeo final aparece em respostas de status posteriores.

## Contrato Público

Use `operation` explícito em produção. O AI Sonar pode inferir algumas operações a partir das entradas, mas valores de operação explícitos tornam a validação, suporte e tentativas mais claros.

| Operação | Entrada necessária ou típica | Caso de uso |
|----------|------------------------------|-------------|
| `text-to-video` | `prompt` | Gerar apenas a partir de texto |
| `image-to-video` | `image_url` ou `image` compatível | Animar uma imagem inicial |
| `reference-to-video` | `reference_images` e `video_urls` / `audio_urls` opcionais em modelos suportados | Manter identidade, estilo ou referências de ativos |
| `start-end-to-video` | `start_image`, `end_image` | Controlar os primeiros e últimos quadros |
| `video-to-video` | `video_url` ou `task_id` específico do modelo | Transformar ou aumentar um clipe existente |
| `motion-control` | `image_url` mais `video_url` | Aplicar referência de movimento a um sujeito |
| `audio-to-video` | `audio_url` | Fluxos de vídeo condicionados por áudio |
| `video-extension` | `task_id`, `extend_at` ou campos de extensão específicos do modelo | Continuar um vídeo gerado |

## Descoberta de Modelos

```bash
curl "https://api.aisonar.dev/v1/models?recommended_for=video" \\
  -H "Authorization: Bearer sk-your-api-key"
```

Use IDs logicos de produto em `model` e escolha variantes com `operation` e os inputs de midia correspondentes. Exemplos: `wan-2.7`, `happyhorse-1.0`, `viduq3`, `viduq3-mix`, `pixverse-v6`, `kling-3.0-video`, `veo3.1` e `seedance-2.0`; nao exponha IDs upstream codificados por operacao como nomes publicos de modelo.

Leia os detalhes do modelo selecionado antes de confiar em campos especializados, como `reference_images`, `kling_elements`, `output_audio`, `duration`, `resolution` ou `aspect_ratio`.

## Criar Solicitação

```bash
curl https://api.aisonar.dev/v1/videos/generations \\
  -H "Authorization: Bearer sk-your-api-key" \\
  -H "Content-Type: application/json" \\
  -d '{
    "model": "veo3.1",
    "operation": "text-to-video",
    "prompt": "Uma cena cinematográfica calma de um gato andando por um jardim ensolarado",
    "duration": 4,
    "aspect_ratio": "16:9"
  }'
```

Para entrada de mídia em produção, prefira URLs públicas `https` em vez de URLs `data:` inline. Se você usar URLs assinadas, mantenha-as válidas durante a criação da tarefa e tentativas de busca do lado do provedor.

## Entradas E Campos Específicos do Modelo

- Solicitações da família Veo 3 padrão para áudio ativado quando `output_audio` é omitido. Defina-o explicitamente quando o modelo suportar a alternância e sua experiência do usuário depender de som.
- `kling_elements` é para solicitações condicionadas por imagem de `kling-3.0-video`. Referencie cada elemento em `prompt` como `@name`; não combine com `output_audio=true`.
- Para a família Seedance 2.0, leia o [guia de modelos de vídeo Seedance 2.0](/pt/guides/seedance-2-video) antes de usar saída 4K, limites Fast/Mini ou entradas de referência multimodais.
- Para `grok-imagine-video`, vídeo-para-vídeo usa um `video_url` público `.mp4`; limites específicos do modelo, como `duration` e `resolution`, devem vir do contrato do modelo.

## Resultados de Polling

Use o `poll_url` retornado primeiro. Se precisar de um endpoint fixo, use `GET /v1/tasks/{id}` com o mesmo `id` / `task_id` da resposta de criação.

Tarefas de vídeo concluídas podem retornar `video_url`, `video` ou `videos`, dependendo do provedor e da contagem de saída. Trate `billing_transaction_id` como um identificador de cobrança, não como um identificador de tarefa.

## Armadilhas Comuns

- Não codifique caminhos de status de vídeo antigos; prefira `poll_url`.
- Não combine campos de primeiro quadro com fluxos dedicados de imagem de referência, a menos que o contrato do modelo permita.
- Não assuma que `duration` descreve o comprimento do vídeo de referência de entrada; geralmente controla o comprimento da saída gerada.
- Não tente recriar solicitações após um timeout sem verificar se uma tarefa já foi criada.

## Referência da API

| Tópico | Referência |
|--------|------------|
| Criar Vídeo | [Criar Vídeo](/pt/api-reference/video/create-video) |
| Obter Status do Vídeo | [Obter Status do Vídeo](/pt/api-reference/video/get-video-status) |
| Obter Status da Tarefa | [Obter Status da Tarefa](/pt/api-reference/tasks/get-task-status) |
| Cancelar Tarefa | [Cancelar Tarefa](/pt/api-reference/tasks/cancel-task) |
| Cobrança & Preços | [Cobrança & Preços](/pt/guides/billing) |